{% extends 'SFCapBundle::cap.html.twig' %}
{% block content %}
<h1>CapGoal creation</h1>

<form action="{{ path('goal_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('goals') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}